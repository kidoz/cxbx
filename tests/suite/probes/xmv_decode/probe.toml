# Exercises the native XMV library through Cxbx's filesystem, D3D/YUY2,
# DirectSound, and timing boundaries. See docs/xmv-architecture.md.
tags = ["xmv", "xdk"]

[env]
CXBX_KERNEL_SKIP_INIT = "1"
CXBX_FS_SWAP = "1"
