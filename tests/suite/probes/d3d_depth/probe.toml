# XDK-toolchain probe (see xdk_smoke): pixel-exact verification of the
# depth-test path (bound depth-stencil surface, depth clear, and
# ZEnable/ZWriteEnable/ZFUNC resolution between overlapping quads).
tags = ["xdk"]

[env]
CXBX_KERNEL_SKIP_INIT = "1"
CXBX_FS_SWAP = "1"
