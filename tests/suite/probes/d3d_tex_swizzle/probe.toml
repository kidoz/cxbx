# XDK-toolchain probe (see xdk_smoke): pins swizzled-texture upload fidelity
# (Morton-order uploads are unswizzled by the HLE's lock write-through since
# 2026-07-14). Expectations encode correct behavior.
tags = ["xdk"]

[env]
CXBX_KERNEL_SKIP_INIT = "1"
CXBX_FS_SWAP = "1"
