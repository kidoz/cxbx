# Requires a target with an NV2A GPU model AND Phase 4 texture sampling (nearest
# + MODULATE, perspective-correct texcoords). On Cxbx the raster path is gated
# behind CXBX_NV2A_RASTER.
tags = ["nv2a"]

[env]
CXBX_NV2A_RASTER = "1"
